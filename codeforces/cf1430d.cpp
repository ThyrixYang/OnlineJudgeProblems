#include <bits/stdc++.h>

using namespace std;

const int MAXN = 1e6;
char ss[MAXN];

int main()
{

    int T, N;
    cin >> T;
    for (int Case = 0; Case < T; Case++)
    {
        cin >> N;
        scanf("%s", ss);
        }

    return 0;
}